# Configuration SentryGuard API
PORT=3001
WEBAPP_URL=http://localhost:4200

# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=sentryguard
DATABASE_PASSWORD=your_database_password_here
DATABASE_NAME=sentryguard
DATABASE_LOGGING=false

# Security
ENCRYPTION_KEY=your_encryption_key_here_min_32_chars

# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_BOT_USERNAME=your_bot_username_here

# Tesla API Configuration (Legacy - pour compatibilité)
ACCESS_TOKEN=your_tesla_access_token_here
LETS_ENCRYPT_CERTIFICATE=your_base64_encoded_certificate_here

# Tesla OAuth Configuration
TESLA_CLIENT_ID=your_tesla_client_id_here
TESLA_CLIENT_SECRET=your_tesla_client_secret_here
TESLA_AUDIENCE=https://fleet-api.prd.eu.vn.cloud.tesla.com
TESLA_REDIRECT_URI=https://sentryguard.org/callback/auth
TESLA_FLEET_TELEMETRY_SERVER_HOSTNAME=https://ws.sentryguard.org

# ZMQ Configuration
ZMQ_ENDPOINT=tcp://10.0.2.12:5284

DEBUG_MESSAGES=true

# Environment
NODE_ENV=development

# Rate Limiting Configuration
# Global default limit (can be overridden per endpoint)
# TTL: Time window in milliseconds (default: 60000ms = 1 minute)
# LIMIT: Number of requests allowed per IP within the TTL window
#
# Note: All rate limiting constants are centrally defined in:
#   apps/api/src/config/throttle.config.ts
#
# These environment variables override the default values from throttle.config.ts
THROTTLE_TTL=60000
THROTTLE_LIMIT=100

# Per-endpoint rate limits (defined in apps/api/src/config/throttle.config.ts):
# - Sensitive public endpoints (OAuth): 40 req/min
# - Authenticated read endpoints: 200 req/min
# - Authenticated write endpoints: 100 req/min
# - Critical/intensive endpoints: 50 req/min
# - Test endpoints: 30 req/min

# JWT
JWT_SECRET=secret-for-jwt-creation
JWT_EXPIRATION=1d
